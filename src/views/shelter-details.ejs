<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= title %></title>
  </head>
  <body>
    <header>
      <h1>Shelter Details</h1>
    </header>

    <h2><%= shelter.name %></h2>

    <main>
      <p><strong>Location:</strong> <%= shelter.location %></p>

      <p>
        <strong>Coordinates:</strong> <%= shelter.latitude %>, <%=
        shelter.longitude %>
      </p>

      <p>
        <strong>Description:</strong> <% if (shelter.description &&
        shelter.description.length > 0) { %> <%= shelter.description.join(', ')
        %>
        <!-- Display as tags -->
        <% } else { %> No description provided. <% } %>
      </p>

      <p>
        <strong>Needs:</strong> <% if (shelter.needs && shelter.needs.length >
        0) { %> <%= shelter.needs.join(', ') %>
        <!-- Display as list of needs -->
        <% } else { %> No current needs. <% } %>
      </p>

      <p>
        <strong>Upvotes:</strong> <span id="upvote-count-<%= shelter.id %>"><%= shelter.upvotes %></span>
      </p>

    </main>

    <a href="/shelters">Back to shelters list</a>

    <footer>
      <p>&copy; 2024 ShelterMap. All rights reserved.</p>
    </footer>

    <script>
      document.getElementById(`upvote-count-${shelterId}`).textContent = data.upvotes;
    </script>
  </body>
</html>