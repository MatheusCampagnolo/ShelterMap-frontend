<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= title %></title>
  </head>
  <body>
    <h1>Available Shelters</h1>

    <ul>
      <% shelters.forEach(shelter => { %>
      <li>
        <strong><%= shelter.name %></strong> - <%= shelter.location %>
        <br />
        <strong>Description:</strong>
        <% if (shelter.description && shelter.description.length > 0) { %> <%=
        shelter.description.join(', ') %>
        <!-- Display as tags -->
        <% } else { %> No description provided. <% } %>
        <br />
        <strong>Needs:</strong>
        <% if (shelter.needs && shelter.needs.length > 0) { %> <%=
        shelter.needs.join(', ') %>
        <!-- Display as list of needs -->
        <% } else { %> No current needs. <% } %>
        <br />
        <a href="/shelters/<%= shelter.id %>">View details</a>
      </li>
      <% }) %>
    </ul>
  </body>
</html>