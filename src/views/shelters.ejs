<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= title %></title>
  </head>
  <body>
    
    <h1>List of Shelters</h1>
    <table>
      <thead>
        <tr>
          <th>Name</th>
          <th>Location</th>
          <th>Latitude</th>
          <th>Longitude</th>
          <th>Description</th>
          <th>Current Needs</th>
          <th>Actions</th>
        </tr>
      </thead>
      <tbody>
        <% shelters.forEach(shelter => { %>
        <tr>
          <td><%= shelter.name %></td>
          <td><%= shelter.location %></td>
          <td><%= shelter.latitude %></td>
          <td><%= shelter.longitude %></td>
          <td><%= shelter.description || 'N/A' %></td>
          <!-- Show description or N/A if not provided -->
          <td>
            <%= shelter.needs.length > 0 ? shelter.needs.join(', ') : 'N/A' %>
          </td>
          <td>
            <a href="/shelters/<%= shelter.id %>/edit">Edit</a>
            <!-- Add any other actions as needed, e.g., delete -->
          </td>
        </tr>
        <% }); %>
      </tbody>
    </table>

  </body>
</html>
