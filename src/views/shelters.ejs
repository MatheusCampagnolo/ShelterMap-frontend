<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= title %></title>
  </head>
  <body>
    <h1>Shelters</h1>
    <ul>
      <% shelters.forEach(shelter => { %>
      <li>
        <a href="/shelters/<%= shelter.id %>"><strong><%= shelter.name %></strong></a>  - <%= shelter.location %> (<%=
        shelter.latitude %>, <%= shelter.longitude %>)
      </li>
      <% }) %>
    </ul>
  </body>
</html>